import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>African Cichlid Clicky Game</h1>
        <p>Click on an image to earn points, but don't click on an image more than once!</p>
    </div>
);

export default Title;
